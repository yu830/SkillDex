# Phase 3 开发进度汇报

## 基本信息

- 项目：SkillDex
- 阶段：Phase 3 - Project Evidence + GitHub Pages Prep
- 日期：2026-07-01
- 当前分支：codex/skilldex-phase-3
- 本阶段 commit：待提交后填写
- GitHub push 状态：待提交后推送 `codex/skilldex-phase-3`
- GitHub 链接：https://github.com/yu830/SkillDex

## 本阶段目标

补齐 SkillDex 作为个人 AI 工程作品入口的可信度：Project evidence schema、项目详情入口、真实链接缺失时的 `TBD` 规则，以及 GitHub Pages 静态部署准备。

## 修改摘要

- Project schema 扩展为 `status`、`tools`、`links`、`evidence`、`highlights`、`updated_at`。
- `projects.json` 保留六个必含项目，并为每个项目增加诚实 evidence / highlights / links 状态。
- Project card 增加同页 `<details>` 详情入口，展示 status、updated date、tools、links、evidence、highlights。
- 项目工具参与 compatible tools 筛选。
- Vite 增加 `base: '/SkillDex/'`，适配 GitHub Pages project-site 路径。
- 新增 GitHub Actions Pages workflow：只在 `main` push 和 `workflow_dispatch` 触发。
- README、schema 文档和内容准则更新为 Phase 3 数据模型。

## 部署准备状态

- 已准备 GitHub Pages workflow：`.github/workflows/deploy-pages.yml`。
- workflow 会执行 `npm ci`、`npm run test`、`npm run build`，然后上传 `dist` 到 Pages。
- feature branch push 不会自动部署；只有 `main` push 或手动 `workflow_dispatch` 会触发。
- 预期 Pages URL：`https://yu830.github.io/SkillDex/`。
- 尚未声称线上可访问；需要合并到 `main`、启用 GitHub Pages from Actions，并验证真实 URL HTTP 200 后才能确认。

## 运行与验证

```bash
npm run test
npm run build
npm audit --audit-level=moderate
npm run dev -- --host 127.0.0.1 --port 5174
Invoke-WebRequest http://127.0.0.1:5174/SkillDex/
gh api repos/yu830/SkillDex/pages
Invoke-WebRequest https://yu830.github.io/SkillDex/
git status
git log --oneline -5
```

## 验证结果

- `npm run test`：通过，4 个 test files，20 个 tests。
- `npm run build`：通过，TypeScript 编译和 Vite production build 成功。
- `npm audit --audit-level=moderate`：通过，0 vulnerabilities。
- 本地 Vite dev server：`http://127.0.0.1:5174/SkillDex/` 返回 HTTP 200。
- GitHub Pages API：`gh api repos/yu830/SkillDex/pages` 返回 404，说明 Pages 当前未配置或不可见。
- 预期线上 URL：`https://yu830.github.io/SkillDex/` 返回 404，不能声明线上可访问。
- Workflow action tags 已用 `git ls-remote --tags` 验证存在：`actions/checkout@v7`、`actions/setup-node@v6`、`actions/configure-pages@v6`、`actions/upload-pages-artifact@v5`、`actions/deploy-pages@v5`。

## 已知缺口

- 项目 repo / demo / docs / caseStudy 仍为 `TBD`，需要用户提供真实链接后才能公开展示。
- 当前 evidence 项已经区分本地状态与缺失公开证明，但真实 artifact path、commit、截图或 case study 仍需后续补充。
- GitHub Pages 只是准备完成；未合并到 `main`，Pages API 和预期 URL 当前均未返回可用站点。
- 未新增 Playwright / Testing Library；详情展开使用原生 `<details>`，核心数据规则由 Vitest 覆盖。

## Phase 4 建议

- 合并到 `main` 后启用并验证 GitHub Pages。
- 补充真实项目链接和截图证据。
- 做桌面/移动端截图与浏览器级交互验证。
- 在不改变整体克制风格的前提下做视觉 polish。
